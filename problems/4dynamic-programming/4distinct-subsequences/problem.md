# 不同的子序列（Distinct Subsequences）

> LeetCode: [115. Distinct Subsequences](https://leetcode.com/problems/distinct-subsequences/) / [115. 不同的子序列](https://leetcode.cn/problems/distinct-subsequences/)

## 题目描述

给你两个字符串 `s` 和 `t`，请统计并返回在 `s` 的**子序列**中 `t`
**出现的个数**。

字符串的一个子序列是指从原字符串中删去若干个（也可以不删）字符，且不
改变剩余字符相对位置后组成的新字符串。（例如 `"ACE"` 是 `"ABCDE"` 的一个
子序列，而 `"AEC"` 不是）

## 输入格式

第一行：字符串 `s`。
第二行：字符串 `t`。

## 输出格式

一行一个整数：`s` 的子序列中 `t` 出现的个数。

## 样例

样例 1：

输入：
```
rabbbit
rabbit
```

输出：
```
3
```

解释：如下所示，有 `3` 种可以从 `s` 中得到 `"rabbit"` 的方案。
```
rabbbit
rabbbit
rabbbit
```

样例 2：

输入：
```
babgbag
bag
```

输出：
```
5
```

解释：如下所示，有 `5` 种可以从 `s` 中得到 `"bag"` 的方案。
```
babgbag
babgbag
babgbag
babgbag
babgbag
```

## 数据范围

- `0 <= s.length, t.length <= 1000`
- `s` 和 `t` 由英文字母组成
- 结果保证能用 `32` 位有符号整数存储

## 提示

- 定义 `dp[i][j]` 表示 `s` 的前 `i` 个字符中，`t` 的前 `j` 个字符作为子序列
  出现的个数。
- 边界：`dp[i][0] = 1`（空字符串是任何字符串的子序列，且只有一种取法：
  什么都不选）；`dp[0][j] = 0`（`j > 0` 时，空的 `s` 无法匹配出非空的
  `t`）。
- 转移：考虑 `s` 的第 `i` 个字符（下标 `i-1`）是否参与匹配 `t` 的第 `j`
  个字符（下标 `j-1`）：
  - 不选它：方案数为 `dp[i-1][j]`（`t` 的前 `j` 个字符仍需从 `s` 的前
    `i-1` 个字符中找）。
  - 若 `s[i-1] == t[j-1]`，还可以选它去匹配：方案数再加上 `dp[i-1][j-1]`。
  - 因此：`s[i-1] == t[j-1]` 时 `dp[i][j] = dp[i-1][j] + dp[i-1][j-1]`；
    否则 `dp[i][j] = dp[i-1][j]`。
- 这是一道二维字符串 DP：状态定义在“前 `i` 个 / 前 `j` 个”上，但这里求的
  是**方案数**而不是长度，转移里的加法对应“选或不选当前字符去匹配”的两
  种方案相加。
- 虽然题目保证答案能用 `32` 位有符号整数存储，但 DP 过程中用 `unsigned
  long long` 存储中间结果更保险，可以避免因为写法差异导致的溢出问题。
- 可以用滚动数组把空间优化到 `O(n)`：从 `j` 的**大到小**遍历更新一维
  数组（类似 0/1 背包的空间优化技巧），避免同一轮内覆盖尚未使用的
  `dp[j-1]` 旧值。

## 函数签名（LeetCode 风格）

```cpp
int numDistinct(string s, string t) {

}
```
