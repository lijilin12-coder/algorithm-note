# 1221. 四平方和

- [题目](https://www.acwing.com/problem/content/description/1221/)
- [提交记录](https://www.acwing.com/problem/content/submission/1221/)
- [讨论](https://www.acwing.com/problem/content/discussion/index/1221/1/)
- [题解](https://www.acwing.com/problem/content/solution/1221/1/)
- [视频讲解](https://www.acwing.com/problem/content/video/1221/)

## 题目描述

四平方和定理，又称为拉格朗日定理：

每个正整数都可以表示为至多 4 个正整数的平方和。

如果把 0 包括进去，就正好可以表示为 4 个数的平方和。

比如：

```
5 = 0^2 + 0^2 + 1^2 + 2^2
7 = 1^2 + 1^2 + 1^2 + 2^2
```

对于一个给定的正整数，可能存在多种平方和的表示法。

要求你对 4 个数排序：

`0 <= a <= b <= c <= d`

并对所有的可能表示法按 `a, b, c, d` 为联合主键升序排列，最后输出第一个表示法。

## 输入格式

输入一个正整数 `N`。

## 输出格式

输出 4 个非负整数，按从小到大排序，中间用空格分开。

## 数据范围

`0 < N < 5 * 10^6`

## 样例

输入：
```
5
```

输出：
```
0 0 1 2
```

## 提示

- 暴力枚举 `a, b, c` 三层再算 `d = sqrt(N - a^2 - b^2 - c^2)`，复杂度接近
  `O(N^{3/2})`，`N` 达到 `5*10^6` 级别会超时，需要用**折半枚举（meet in the
  middle）**。
- 把四个数拆成两半：`(c, d)` 和 `(a, b)`，满足 `c^2 + d^2 + a^2 + b^2 = N`，
  即 `c^2 + d^2 = N - a^2 - b^2`。
- **预处理**：枚举所有 `0 <= c <= d` 且 `c^2 + d^2 <= N` 的组合，按平方和
  `s = c^2 + d^2` 建一张哈希表；因为 `c` 从小到大枚举，同一个 `s` 第一次出现
  时记录的 `c` 就是这个 `s` 下最小的 `c`（这一步保证了后续查表时优先取到字典序
  更小的 `c`）。
- **枚举查表**：枚举 `0 <= a <= b`（利用 `4a^2 <= N`、`3b^2 <= N - a^2` 剪枝
  上界），计算剩余 `t = N - a^2 - b^2`；若哈希表中存在 `t`，取出对应的 `c`，
  只有当 `c >= b` 时这组解才满足 `a <= b <= c <= d`，此时 `d = sqrt(t - c^2)`
  即为答案，直接输出并结束（因为 `a, b` 是从小到大枚举的，第一次命中就是
  字典序最小的解）。
- 注意用 `long long` 或谨慎处理 `sqrt` 精度问题（先转 `double` 开方再四舍五入
  取整验证）。
