# 730. 机器人跳跃问题

- [题目](https://www.acwing.com/problem/content/description/730/)
- [提交记录](https://www.acwing.com/problem/content/submission/730/)
- [讨论](https://www.acwing.com/problem/content/discussion/index/730/1/)
- [题解](https://www.acwing.com/problem/content/solution/730/1/)
- [视频讲解](https://www.acwing.com/problem/content/video/730/)

## 题目描述

机器人正在玩一个古老的基于 DOS 的游戏。

游戏中有 `N+1` 座建筑——从 `0` 到 `N` 编号，从左到右排列。

编号为 `0` 的建筑高度为 `0` 个单位，编号为 `i` 的建筑高度为 `H(i)` 个单位。

起初，机器人在编号为 `0` 的建筑处。

每一步，它跳到下一个（右边）建筑。

假设机器人在第 `k` 个建筑，且它现在的能量值是 `E`，下一步它将跳到第 `k+1` 个建筑。

如果 `H(k+1) > E`，那么机器人就失去 `H(k+1) - E` 的能量值，否则它将得到 `E - H(k+1)`
的能量值。

游戏目标是到达第 `N` 个建筑，在这个过程中能量值不能为负数个单位。

现在的问题是机器人至少以多少能量值开始游戏，才可以保证成功完成游戏？

## 输入格式

第一行输入整数 `N`。

第二行是 `N` 个空格分隔的整数，`H(1), H(2), …, H(N)` 代表建筑物的高度。

## 输出格式

输出一个整数，表示所需的最少单位的初始能量值上取整后的结果。

## 数据范围

`1 <= N, H(i) <= 10^5`

## 样例

输入样例 1：
```
5
3 4 3 2 4
```

输出样例 1：
```
4
```

输入样例 2：
```
3
4 4 4
```

输出样例 2：
```
4
```

输入样例 3：
```
3
1 6 4
```

输出样例 3：
```
3
```

## 提示

- 关键推导：设机器人到达第 `k` 座建筑时能量为 `E`，跳到第 `k+1` 座建筑（高度
  `H(k+1)`）后的新能量：
  - 若 `H(k+1) > E`：新能量 `= E - (H(k+1) - E) = 2E - H(k+1)`；
  - 若 `H(k+1) <= E`：新能量 `= E + (E - H(k+1)) = 2E - H(k+1)`。
  两种情况公式相同，恒有 `E_new = 2 * E - H(k+1)`。
- **单调性**：初始能量越大，每一步之后的能量也不会更小，因此“初始能量 `E` 能否
  保证全程非负”是关于 `E` 单调的——若 `E` 可行，则所有 `E' > E` 也可行。这满足
  二分答案的条件。
- 因此可以**二分初始能量值** `E`（整数，范围 `[0, 10^5]`），对每个候选值模拟一遍
  跳跃过程，只要中途能量为负就判定不可行。
- 剪枝：一旦某一步能量 `>= max(H(i))`，之后能量只会不减且不再可能变负，可以提前
  判定后续必然成功，避免数值持续增长。
