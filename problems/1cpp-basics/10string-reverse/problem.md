# 反转字符串

## 题目描述

给定一个字符串 `s`（不含空格），请你输出反转后的结果。

## 输入格式

一行，一个字符串。

## 输出格式

一行，反转后的字符串。

## 样例

输入：
```
hello
```

输出：
```
olleh
```

## 数据范围

- 字符串长度不超过 `1000`。

## 提示

- `string` 和 `vector` 一样支持迭代器，直接用 `reverse(s.begin(), s.end())` 反转。
